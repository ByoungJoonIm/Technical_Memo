## 함수 사용법
- sha256
  ```
  import hashlib
  rs = hashlib.sha256("foobar").hexdigest()
  print(rs)
  ```          
  
## PIP
- pygraphviz 설치
  - $ `apt update`
  - $ `apt install graphviz graphviz-dev`
  - $ `pip install pygraphviz`
